# 사이트 실시간 재고 관리 — 설치 안내

booking.html에 "사이트 직접 선택 + 실시간 재고 관리" 기능을 추가했습니다. 재고는 구글 스프레드시트에 저장되고, Apps Script가 조회/예약 API 역할을 합니다. 아래 순서대로 딱 한 번만 설정하면 됩니다.

## 1. 스프레드시트 만들기
1. sheets.google.com에서 새 스프레드시트를 만듭니다. (이름 예: `호캠프 리턴즈 사이트 재고`)

## 2. Apps Script 붙여넣기
1. 스프레드시트 메뉴에서 **확장 프로그램 → Apps Script** 를 클릭합니다.
2. 기본으로 열려있는 `Code.gs`의 내용을 전부 지우고, 함께 전달한 `Code.gs` 파일 내용을 그대로 붙여넣습니다.
3. 저장합니다 (Ctrl/Cmd + S).

## 3. 사이트 목록 생성
1. 상단 함수 선택 드롭다운에서 `initSites`를 선택합니다.
2. ▶ 실행 버튼을 누릅니다. 처음 실행 시 권한 승인 창이 뜨면 승인해 주세요.
3. 스프레드시트로 돌아가면 `사이트재고` 탭이 생기고, 아래 구역 기준으로 사이트 57개가 자동으로 채워져 있습니다.

   - A구역: A-1 ~ A-20 (20개, 일반사이트)
   - C구역: C-23 ~ C-30 (8개, 일반사이트) / C-31 ~ C-38 (8개, **캠핑카·카라반·트레일러 전용 대형 사이트**, 참가비 +20만원 — 예약 화면 보기 옵션에는 가격을 표시하지 않고 접수 요약/참가비 안내에서만 반영됩니다) + C-30-1, C-30-2 (2개, 30번 자리를 분할한 개별 사이트)
   - D구역: D-41 ~ D-42 (2개)
   - F구역: F-50 ~ F-54 (5개)
   - G구역: G-57 (1개)
   - H구역: H-58 ~ H-68 (11개)

   예약 화면에서는 같은 구역 안에 타입이 여러 개 있으면(C구역처럼) 자동으로 소그룹으로 나뉘어 표시됩니다.

   구역/번호가 바뀌면 `Code.gs` 상단의 `ZONES` 값을, 30-1/30-2처럼 번호 규칙에서 벗어나는 사이트는 `EXTRA_SITES` 값을, 특정 구간의 타입/가격을 다르게 주고 싶으면 `SPECIAL_SITE_RANGES` 값을 수정한 뒤 `initSites`를 다시 실행하세요. (다시 실행하면 시트가 초기화되니, 이미 예약이 들어온 시즌 중에는 실행하지 말고 시트에 행을 직접 추가/수정하세요.) booking.html의 `EXTRA_SITES_FALLBACK` / `isSpecialSite()`도 함께 맞춰두면 데모 모드 화면과 어긋나지 않습니다.

## 4. 웹앱으로 배포
1. Apps Script 편집기 오른쪽 위 **배포 → 새 배포** 클릭
2. 유형: **웹 앱** 선택
3. "실행 사용자": **나(본인 계정)**
4. "액세스 권한이 있는 사용자": **모든 사용자**
5. 배포를 누르고, 생성된 **웹앱 URL**을 복사합니다.

## 5. booking.html에 URL 연결
`booking.html` 파일을 열어 `<script>` 태그 상단 근처에서 아래 줄을 찾습니다.

```js
var SITE_API_URL = 'PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE';
```

따옴표 안을 4번에서 복사한 웹앱 URL로 바꿔주세요. 저장 후 booking.html을 실제 배포 위치(예: 웹호스팅, 깃허브 페이지 등)에 올리면 완료입니다.

> URL을 연결하기 전까지는 데모 모드로 동작합니다 (재고 체크 없이 임시 사이트 목록만 보여줌). 실제 신청을 받기 전에 꼭 URL을 연결해 주세요.

## 사용법 요약
- **사이트별 가격 조정**: `사이트재고` 시트에서 해당 행의 `추가금액(원)` 값을 바꾸면 됩니다. 방갈로처럼 비싼 자리는 양수(예: 70000), 할인 자리는 음수(예: -20000)를 넣으면 참가비 계산에 자동 반영됩니다.
- **사이트 타입 표시**: `타입` 열 값을 "파쇄석", "데크", "방갈로" 등으로 바꾸면 접수 요약/구글폼에 함께 표시됩니다.
- **강제 마감 처리**: 특정 사이트를 미리 막아두고 싶다면 `상태` 열을 `예약완료`로 바꾸면 예약 화면에서 회색으로 비활성화됩니다.
- **예약 취소 시 재고 복원**: 참가자가 취소하면 해당 행의 `상태`를 다시 `예약가능`으로, `예약자명/연락처/예약시각`은 비워주세요.
- **동시 접속 중복 예약 방지**: 두 사람이 같은 사이트를 동시에 선택해도 Apps Script의 잠금(Lock) 처리로 한 명만 성공하고, 나머지는 "이미 예약된 사이트입니다" 안내를 받고 자동으로 최신 재고를 다시 불러옵니다.
- **매년(시즌) 초기화**: 새 시즌마다 `ZONES` 값을 갱신하고 `initSites`를 다시 실행하면 전체 초기화됩니다.
